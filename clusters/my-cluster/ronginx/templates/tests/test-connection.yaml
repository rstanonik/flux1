apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ronginx.fullname" . }}-test-connection"
  labels:
    {{- include "ronginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ronginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
