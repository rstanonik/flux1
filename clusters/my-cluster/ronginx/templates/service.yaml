apiVersion: v1
kind: Service
metadata:
  name: {{ include "ronginx.fullname" . }}
  labels:
    {{- include "ronginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ronginx.selectorLabels" . | nindent 4 }}
